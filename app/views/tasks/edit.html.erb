<h1>id:
<%= @task.id %>
の編集ページ
</h1>

<%= form_for(@task) do |t| %>

<%= t.label :status, '状態' %>

<%= t.text_field :status %>

<%= t.label :content, '予定' %>

<%= t.text_field :content %>

<%= t.submit '書き込む' %>

<% end %>

<%= link_to '一覧に戻る', tasks_path %>